spring:
  application:
    name: disaster-integrator-test

  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect

  servlet:
    multipart:
      enabled: true
      max-file-size: 50MB
      max-request-size: 50MB

security:
  jwt:
    secret: test-secret-key-for-disaster-resilience-hub

logging:
  level:
    com.disaster.integrator: DEBUG
    org.springframework.web: DEBUG

data:
  validation:
    max-temperature: 60.0
    min-temperature: -90.0
    max-magnitude: 10.0
    min-magnitude: 0.0

# Fixed key so the end-to-end test is deterministic: ciphertext written in one test
# method must still decrypt in the next. An unset key generates a fresh one per boot.
privacy:
  encryption:
    key: ZHJoLXRlc3Qtb25seS1rZXktMzItYnl0ZXMtbG9uZyE=
  retention:
    location-history-days: 90
    cron: "0 0 3 * * *"
