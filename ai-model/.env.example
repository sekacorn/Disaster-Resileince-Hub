# AI Prediction Service Configuration

# Server Configuration
HOST=0.0.0.0
PORT=8000
WORKERS=-1  # -1 for auto-detect based on CPU cores

# Model Configuration
MODEL_PATH=models/disaster_risk_model.pth

# Logging
LOG_LEVEL=INFO

# CORS (adjust for production)
ALLOWED_ORIGINS=*

# PyTorch Configuration
TORCH_DEVICE=cpu  # or cuda for GPU support
